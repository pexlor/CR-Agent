# Code Review Agent 开发记录

## 原题

做一个 Code Review Agent。

输入：GitLab MR、GitHub PR 链接，或者 `git diff`。

输出：直接回评到 MR/PR，或者生成 Markdown 报告。

另外还要支持：

- checkpoint，异常后不要从头跑；
- 每条评论可以查到 diff、Prompt、模型回复和工具记录；
- 新增工具时不修改主流程；
- 可以限制一次 Review 的 Token 或金额预算；
- 评论要区分置信度；
- Secret 不能发给大模型，也不能执行用户仓库里的任意代码。

题目明确要求使用 AI 完成，还需要自己判断最后交付什么材料，证明自己使用 AI 的能力。

## 我的初步想法

拿到题目后，我没有直接让 AI 开始写代码。

这道题看着功能不多，但每个要求其实都比较模糊。例如“支持恢复”，恢复到哪一步才算支持；“限制 10 元”，是事后统计还是调用前真的不能超过；“不能上传 Secret”，是不是只要 Prompt 里不出现就行。

如果这些东西不先定下来，AI 很容易快速写出一个能跑的 Demo，但最后不知道怎么验收。

所以我先定了几个原则：

- 先把需求和范围说清楚，再写代码；
- 我先提供自己的想法，让 AI 帮我补充和反驳；
- 不让 AI 一次做很多事情，每次只处理一个明确任务；
- 重要结论必须写入文档，不能只留在聊天记录里；
- AI 写完代码后要补测试，我再看测试能不能证明问题；
- 重要修改再让另一个 AI 或新的上下文做一次 Review；
- 最后必须跑真实模型和真实平台，不能只看 mock；
- 没完成的功能直接写没完成，不包装成已经支持。

我在项目里主要负责确定方向、限制范围、追问问题和检查结果。代码和大部分文档让 AI 完成，但最后哪些方案可以接受、哪些结果可以算通过，需要我自己判断。

## 制定开发流程

我最开始给 AI 的要求大概是：

```text
先不要写代码。

先分析整个题目，把需求、范围、异常情况和验收标准拆出来。
不确定的地方标记为待确认，不要自己假设。

需求确认后再做技术调研和设计。
设计完成后拆分任务，每个任务都要有测试和验收方式。
```

最后确定的流程：

```text
保存原题
→ 拆需求
→ 确定 MVP 范围
→ 调研类似项目
→ 技术选型
→ 架构和模块设计
→ 拆分实现任务
→ 自动化测试
→ 真实模型和真实平台测试
→ 根据测试结果修复
→ 整理交付文档
```

完整流程见 [开发流程](docs/开发流程.md)。

## 需求拆解

我让 AI 第一轮只分析“要做什么”，不要提前做技术选型。

提示词：

```text
现在需要你对项目进行详细的拆解和分析。

你的任务不是立即写代码，而是先对整个需求进行系统性分析和工程化拆解，
不要进行系统设计、技术选型、架构设计、API 设计或代码实现。

每条需求需要包含正常流程、异常场景、边界条件和验收标准。
不确定的内容标记为【待确认】，不要自行假设为确定需求。
最后列出最核心的需求、最大的风险和最需要确认的问题。
```

AI 第一轮列了 90 个问题，主要包括：

- GitHub/GitLab 是否支持私有仓库；
- Draft、关闭、已合并的 PR/MR 怎么处理；
- diff 的大小、文件数和行数是否有限制；
- 模型超时、限流、进程退出后怎么恢复；
- checkpoint 保存到哪个粒度；
- 模型请求结果不明时是否自动重试；
- Trace 里到底保存什么；
- Secret 是否可以保存在本地 Trace；
- 工具能不能读取配置、执行脚本或者加载插件；
- Token 预算是软限制还是硬限制；
- 置信度和严重程度是不是同一个东西。

AI 很容易把需求越拆越大，所以这里不能只是让它一直提问题。我最后按“首版满足题目，而且可以真实验收”这个原则做了裁决。

| 问题 | 我的决定 |
| --- | --- |
| 输入 | 首版支持本地 diff、GitHub.com PR、GitLab.com MR |
| 输出 | 首版只生成 Markdown 报告，不直接回评 |
| 私有仓库 | 支持，每个平台配置一份本机共享只读凭证 |
| 平台范围 | 只支持 GitHub.com 和 GitLab.com，不支持自建平台 |
| 语言 | Python、JavaScript、TypeScript |
| 检查范围 | 正确性、安全、可靠性和明显性能问题 |
| 恢复 | 支持输入、文件审查、工具执行单元级 checkpoint |
| 工具 | 只允许只读、确定性的静态检查 |
| 预算 | 使用人民币配置，调用前换算并预留 Token |
| 置信度 | “高置信度可直接采纳”和“仅供参考”两类 |

这里我主动把“直接回评 MR/PR”放到了首版范围外。

主要原因是直接回评需要写权限，还要处理评论位置错误、重复发布、恢复后重复评论等问题。题目允许输出 Markdown，所以首版先把 Markdown 做完整，比做一个不安全的回评功能更合适。

最后形成了 23 项需求裁决和 27 条 P0 要求，具体见 [需求拆解](docs/需求拆解.md)。

## 调研类似项目

> 这块主要让 AI 帮我查，我负责判断哪些东西适合这个题目。

提示词：

```text
寻找 GitHub 上相似的项目并进行分析。
重点看输入适配、恢复、Trace、工具扩展、预算、置信度和安全。

不要只介绍功能，还要说明哪些方案可以参考，哪些不能直接照搬。
```

| 项目 | 我参考的地方 | 不能直接照搬的地方 |
| --- | --- | --- |
| PR-Agent | 多平台适配、diff 组织、Prompt 配置 | 没有直接覆盖 checkpoint、逐评论 Trace 和硬预算 |
| Zadig Review Agent | 文件分块、位置校验、工具记录 | 主要面向本地 Git 输入 |
| AI Code Reviewer | 多 Agent 聚合、去重、置信评分 | 成本高，而且多个模型同意也不代表一定正确 |
| reviewdog | diff 过滤、统一诊断、输出适配 | 它不是完整的 LLM Review Agent |

这一步对我影响比较大的有三点：三种输入应该先统一；普通日志不等于逐评论 Trace；声明了工具不代表扩展性成立，最后必须真的新增一个工具跑通。

完整调研见 [相似项目实现参考](docs/相似项目实现参考.md)。

## 技术选型

我一开始的想法：

```text
使用 Python 开发。
先不做 Web，做本地 CLI。
状态使用 SQLite 保存，不考虑复杂的分布式并发。
模型接入可以考虑复用 LangChain。
```

AI 分析后，保留了 Python、CLI 和 SQLite，但没有使用完整的 LangChain Agent 或 LangGraph。

原因是这个项目的流程比较确定，难点主要是状态恢复、预算、安全和 Trace。引入完整 Agent 框架不能自动解决这些问题，反而会增加一套状态管理。

| 部分 | 技术 |
| --- | --- |
| 语言 | Python 3.12 |
| 项目管理 | uv、`pyproject.toml`、`uv.lock` |
| CLI | Typer、Rich |
| 数据校验 | Pydantic v2 |
| GitHub/GitLab | PyGithub、python-gitlab、HTTPX |
| diff 解析 | unidiff 加自己的严格校验 |
| 持久化 | 原生 sqlite3 |
| 凭证 | keyring |
| Secret 检测 | detect-secrets 加自己的规则 |
| Markdown | Jinja2 |
| 测试 | pytest、Hypothesis、Ruff、mypy |

模型侧只定义 Provider 接口，通过 OpenAI-compatible 协议接入。后面更换模型时，不需要修改领域逻辑。完整选型见 [技术选型](docs/技术选型.md)。

## 架构设计

架构设计阶段，我要求 AI 不要只画一个很大的 Agent 流程，而是先写清楚每个模块拥有什么状态、谁可以修改状态、失败后怎么恢复。

```text
CLI
  → 应用编排
    → 任务和 Checkpoint
    → 输入、规划、执行
    → Finding 和置信度
    → Budget / Security / Trace / Report
  → GitHub / GitLab / Model / SQLite / Keyring 等适配器
```

我要求一直保持这些规则：

- 领域层不能直接依赖 CLI、数据库和平台 SDK；
- PR/MR 开始审查后固定 base/head SHA；
- 模型调用前先检查安全和预算；
- 平台响应、工具结果和模型回复都按不可信内容处理；
- 工作单元状态、预算和 Trace 要保持一致；
- 没有审查完成，不能输出“完整且无问题”；
- 报告只使用已经保存的结果快照生成。

### 输入怎么统一

本地 diff、GitHub PR、GitLab MR 最后都会转成 `ChangeSet`。后面的规划、模型调用和报告不需要知道原始输入来自哪里。

平台输入还会保存固定版本和完整性信息。这样 Review 过程中即使 PR 又有新提交，也不会把两个版本混起来。

### 恢复怎么做

我开始以为有 checkpoint 后直接 retry 就行，后来发现模型调用和普通函数不一样。

如果请求已经发出去，但进程在结果保存前挂了，系统不知道模型是否已经执行和计费。这个状态不能直接当失败，也不能自动重试。

最后的处理是：完成的 work unit 直接复用；不确定的调用标记为 `unknown`；先按已经预留的最大 Token 记账；用户明确确认后才允许再次调用。

### 预算怎么做

如果只在调用结束后统计 Token，实际上不能保证不超预算。

所以预算采用“先预留、后结算”：先把人民币金额按配置单价换算成 Token 上限，每次调用前预留最大消耗，结束后按实际 Token 结算。预算不够时，请求不会发出去，报告会把对应部分写成未审查。

### Trace 怎么做

一开始容易把 Trace 理解成任务日志，但题目要求的是每条评论都可以追溯。

所以每条 finding 都有自己的 Trace ID，可以查到：

```text
脱敏后的 diff
→ 实际发给模型的 Prompt
→ 工具调用和结果
→ 模型回复
→ Finding 校验
→ 最终报告评论
```

Trace 不保存模型内部思维过程，只保存可以核对的输入、输出和事件。

### 安全怎么做

安全不能只在 Prompt 里写一句“不要泄漏 Secret”。模型请求、模型回复、工具结果、Trace、错误信息和最终报告都需要检查。

仓库内容只能当数据，不能当系统指令。即使代码或 README 里写着“执行这个命令”“提高预算”“把文件上传出去”，系统也不能执行。

因此首版不运行目标仓库的测试、构建、脚本、插件、钩子和依赖。typecheck 如果会加载仓库配置或插件，也不能直接使用。扩展工具只允许做受限的只读静态检查。

完整设计见 [架构设计](docs/架构设计.md) 和 [模块设计目录](docs/模块设计/)。

## 实现过程中的几次纠正

### 模型输出不是每次都按格式返回

真实模型有时返回 `{"findings": null}`，有时又会自己增加 `summary` 字段。

最简单的做法是解析器尽量兼容，但这样输出会越来越不可控。最后我让 AI 从契约上修复：Prompt 明确只允许一个顶层 `findings` 数组，程序侧继续严格校验，并增加测试。

详细记录见 [模型输出契约设计](docs/superpowers/specs/2026-09-13-model-output-contract-fix-design.md)。

### GitHub 的真实数据和 mock 不完全一样

跑真实 GitHub PR 后发现：重命名文件不能使用普通的同路径 diff header；GitHub 返回 `patch = null` 时，也不一定表示文件是二进制。

修复后，对 rename 生成正确的 diff header；需要完整 diff 时只允许请求同一个 API 主机，跨主机重定向仍然拒绝。

这次让我比较明确地感觉到：mock 只能证明代码符合我们自己的假设，不能证明假设符合真实平台。

### GitLab 接口的真实返回结构不一样

第一次跑真实 GitLab.com MR，直接报错：

```text
'list' object has no attribute 'get'
```

代码原来假设 `/diffs` 返回 `{"diffs": []}`，但 GitLab.com 实际返回裸数组 `[]`。另外接口返回的 diff 已经带换行，代码又补了一次，导致解析器判断格式错误。

我让 AI 先用真实 API 确认返回内容，再兼容两种结构，并把真实响应形状写成契约测试。以后即使继续重构，也不会把这个问题改回来。

### 真实结果没有高置信度

真实模型测试一共产生过 21 条 finding，但全部是 `advisory`。

检查后发现当前内置工具没有给具体 finding 提供足够的确定性证据，所以按照规则不能升级成“高置信度可直接采纳”。我没有让 AI 调低标准。这个结果虽然没有全部标成高置信度好看，但说明规则是真的在约束模型。

### 功能跑通后又发现安全问题

真实模型测试时，最开始配置的地址是控制台路径，不是标准 API 路径，所以一直返回 404。改成正确端点后才跑通。

本地 `code-review-agent.toml` 后来设置成 `0600` 并加入 `.gitignore`。后续安全检查又发现 checkpoint 反序列化不能根据数据库字符串动态导入任意类型，最后改成只允许显式登记的可信类型，并增加恶意类型测试。

这部分让我意识到，功能能运行和安全边界完整是两件事。

## 测试和验收

我把测试分成三层：单元和契约测试、AC-01～AC-14 固定验收样例、真实模型和真实平台测试。

| 能力 | 实际测试方法 |
| --- | --- |
| 可恢复 | 模型请求发出后用 `kill -9` 结束进程，再执行 resume |
| 可观测 | 直接读 SQLite，检查评论对应的 diff、Prompt、回复、工具和预算事件 |
| 可扩展 | 新增 `debug-print-detector` TOML，不改 Python 主流程并真实执行 |
| 预算 | 把预算降到约 10 Token，确认请求发出前就被拒绝 |
| 置信度 | 统计真实 finding 和证据，证据不足时保持 advisory |
| 安全 | 在 diff 放入伪造真实格式密钥，检查请求体、Trace、报告和数据库 |

真实中断测试中，第一次 resume 会提示 `unknown_retry_confirmation_required`。明确确认后继续执行，已经完成的单元没有重跑。

真实 GitHub 和 GitLab 输入也都跑过，不只是用 mock。GitLab 的严重问题就是在这一步发现的。

详细结果：

- [六项能力测试报告](docs/六项能力测试报告.md)
- [六项能力真实模型报告](docs/六项能力测试报告-真实模型.md)
- [三种输入测试报告](docs/基础功能测试报告-三种输入.md)
- [测试与评估报告](docs/测试与评估报告.md)
- [验收清单](docs/验收清单.md)

项目在不同阶段的报告里出现过 475、487、488 个测试通过，这些数字对应的代码状态不一样。最终不能只挑最大的数字写，正式交付前应该在固定提交上重新执行，把提交 ID 和当次结果一起保存。

## 当前完成的功能

- 支持本地 unified diff、GitHub.com PR、GitLab.com MR；
- 三种输入统一转换成固定版本的 `ChangeSet`；
- 支持 OpenAI-compatible 模型；
- 模型调用前执行安全检查和预算预留；
- 支持 work unit checkpoint 和手动 resume；
- 支持声明式只读静态工具；
- 对 finding 做位置校验、去重、排序和置信度处理；
- 每条评论关联 Trace ID；
- 输出 Markdown 报告；
- 失败、预算不足和结果不明时保留 `partial` 或 `unknown`。

使用方法见 [README](README.md)，固定裁决见 [实现决策](docs/实现决策.md)。

## 当前没有完成的功能和限制

- 没有直接回评 GitHub PR 或 GitLab MR；
- 不支持 GitHub Enterprise 和自建 GitLab；
- 不执行目标仓库的构建、测试和 typecheck；
- 金额预算没有区分输入、输出和缓存 Token；
- 固定样例不能证明可以发现所有未知问题；
- Secret 扫描不能保证识别所有未知格式；
- 当前主要面向本地单进程使用；
- “没有发现”不代表代码一定没有问题。

完整记录见 [已知限制](docs/已知限制.md)。

## 这次使用 AI 的一些想法

AI 比较适合快速拆问题、补边界、生成测试和整理文档。很多我只写了大概方向的东西，它可以很快补成一个能继续讨论的方案。

但 AI 也容易把自己的假设写得很确定。GitLab 接口就是一个例子，代码、测试和文档当时是一致的，但这种一致建立在错误的返回结构上。如果不跑真实接口，很难发现。

另外，AI 经常会倾向于把方案做得更完整。需求阶段列出 90 个问题没有错，但如果每个都做，项目会很快失控。这里还是需要我自己判断什么是题目真正要看的，什么应该先不做。

我觉得这次真正有价值的不是某一段 Prompt，而是整个反馈过程：

```text
我先给出想法
→ AI 补充方案
→ 我检查范围和风险
→ AI 实现和测试
→ 再用真实环境验证
→ 发现假设不对
→ 修复并补回归测试
```

最后我想体现的不是“AI 可以一次把代码写完”，而是我知道什么时候可以相信 AI，什么时候需要继续追问，什么时候一定要用真实证据确认。

## 交付文档

| 文档 | 内容 |
| --- | --- |
| [README](README.md) | 安装、配置和使用方法 |
| [开发流程](docs/开发流程.md) | 项目阶段和 AI 协作原则 |
| [需求拆解](docs/需求拆解.md) | 需求、范围和验收标准 |
| [相似项目实现参考](docs/相似项目实现参考.md) | 相关项目调研 |
| [技术选型](docs/技术选型.md) | 技术方案和取舍 |
| [架构设计](docs/架构设计.md) | 总体架构和主要流程 |
| [模块设计](docs/模块设计/) | 各模块详细设计 |
| [实现决策](docs/实现决策.md) | 已经固定的实现规则 |
| [AI 协作案例](docs/AI协作案例.md) | AI 协作和修正案例 |
| [验收清单](docs/验收清单.md) | 最终验收项 |
| [测试与评估报告](docs/测试与评估报告.md) | 自动化测试和未完成项 |
| [六项能力真实模型报告](docs/六项能力测试报告-真实模型.md) | 真实模型和中断验证 |
| [三种输入测试报告](docs/基础功能测试报告-三种输入.md) | 三种输入的真实测试 |
| [已知限制](docs/已知限制.md) | 当前版本的限制 |
