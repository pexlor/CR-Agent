# CR-Agent 提交说明（致 HR / 面试官）

这是 Code Review Agent Take-Home 作业的提交说明，作为整个材料包的入口。

## 一、项目一句话说明

CR-Agent 是一个本地 Python CLI：接收本地 `git diff`、GitHub.com PR 或
GitLab.com MR，统一转换为内部 `ChangeSet` 后调用大模型逐单元审查，输出带
置信度分级和 Trace 的 Markdown 报告，支持异常后 checkpoint 恢复、金额预算
硬控、显式授权的远程回评，以及声明式静态检查工具扩展。

- 运行方式：`README.md`
- 完整设计：`docs/架构设计.md`、`docs/模块设计/`
- 需求裁决过程：`docs/需求拆解.md`

## 二、六项题目能力，逐项对应到代码和证据

| 能力 | 关键实现 | 验证证据 |
| --- | --- | --- |
| 可恢复（checkpoint） | `application/task_service.py`（`resume()`）、`application/checkpoint_types.py`（受限反序列化白名单） | 真实 `kill -9` 进程后 `resume`：`docs/六项能力测试报告-真实模型.md` 第 1 节 |
| 可观测（trace） | `domain/trace/service.py`、`domain/trace/schemas.py`；CLI `trace show` | 直接从 SQLite 读原始工件核对 trace 与实际发送内容一致：同上报告第 2 节 |
| 可扩展（声明式工具） | `adapters/tools/registry.py`（`ToolRegistry.from_builtin()`）、`adapters/tools/runtime.py`（只读执行边界） | 现场手写新工具 `debug-print-detector`，仅 TOML 声明接入生产 runtime，未改任何主流程代码：同上报告第 3 节 |
| Token 预算 | `domain/budget/ledger.py`、`domain/budget/service.py`、CNY→token 换算见 `config.py` | 预算压到约 10 token，trace 中无任何 `model.call_started`，证明拦截发生在请求发出前：同上报告第 4 节 |
| 置信度分级 | `domain/findings/processor.py`（`direct and (tool_verified or semantic_verified)` 判定规则） | 21 条真实发现按规则如实全部落在 `advisory`（因未接入真 typecheck 工具），没有伪装成高置信度：同上报告第 5 节 |
| 安全（secret 过滤 + 不跑任意代码） | `domain/security/service.py`、`domain/security/policy.py`、`resources/security_policy.toml` | 构造真实格式 Stripe key/连接串，直读发给模型的原始请求体确认已替换为 `<REDACTED:...>`，全库 grep 原始密钥零匹配：同上报告第 6 节 |

## 三、证明「AI 使用能力」的核心材料（建议 HR 按顺序看）

1. **`vibeCoding记录.md`** —— 完整开发日记，含具体 prompt 示例、AI 给出的
   方案、我的裁决理由。看这一份即可了解协作方式和判断逻辑。
2. **`docs/AI协作案例.md`** —— 精选的单任务案例：真实 GitHub PR 验证暴露
   rename diff 处理错误、模型偶发返回异常 JSON 字段，人工定位后收紧契约而非
   放宽兜底，体现工程判断而非无脑接受 AI 输出。
3. **`docs/六项能力测试报告-真实模型.md`** —— 六项能力的真实模型/真实中断
   端到端验证（非 mock），含具体 trace 事件计数、task ID、sqlite 直读结果。
4. **`docs/相似项目实现参考.md`** —— 对 PR-Agent、Zadig、AI Code Reviewer、
   reviewdog 四个开源项目的源码级调研，附精确到行号的引用，说明哪些可参考、
   哪些不能照搬。
5. **`docs/开发流程.md`** —— 协作原则和分阶段执行流程，说明"先定义可验证
   标准、再分任务、再验证"的方法论。

## 四、测试与验收现状（如实说明，不美化）

- 自动化测试：当前 HEAD 522 passed（`uv run pytest -q`，本次复验实测）。
- 测试文件 74 个，共 11309 行；含 `unit`、`contract`、`integration`、
  `acceptance`、`architecture` 等分层。
- `docs/验收清单.md` 是一份逐条勾选的 27 项 P0 + 4 项安全门禁验收表，已按
  现有证据回填。**如实结论是"不通过"**，原因：
  - FR-010 远程回评：GitHub.com 真实发布已验证（Draft PR 真实评论、重试
    幂等），**GitLab.com 真实写入尚未执行**（仅有模拟 HTTP 契约测试）。
  - FR-025～FR-028 四项安全门禁中，`secret` 脱敏和不执行仓库代码有真实验证，
    但"进程/系统调用观测"和部分边界场景（如提示注入抗性的系统级验证）缺乏
    专项证据，按验收清单规则不能算通过。
- 这份清单本身也是材料的一部分：它证明了我们不会把"测试覆盖"和"真实验收"
  混为一谈，也不会为了好看而把未验证的项目打勾。

## 五、已知限制

见 `docs/已知限制.md`，核心几条：
- 只支持 `github.com`/`gitlab.com`，不支持企业自建平台；
- 远程回评不支持编辑/删除已有评论，不自动提交 approve/request-changes；
- 不执行被审查仓库的构建、测试、脚本、插件或依赖；
- 金额预算目前只支持 CNY 单一综合单价，不区分输入/输出/缓存 token 差异化计价。

## 六、如何验证材料本身没有说谎

- 所有"真实模型/真实平台"验证的 task ID、trace 事件计数、SQLite 查询结果
  都可以在对应报告里核对，不是复述模型自评或口头结论。
- `docs/验收清单.md` 中每一条通过标记都注明了证据来源文件和具体数据，未
  找到证据的项保持未勾选状态，而不是全部打勾。
- 建议面试官抽查方式：任选 `docs/六项能力测试报告-真实模型.md` 中一节，
  按文中给出的 task ID 或命令自行复现。
